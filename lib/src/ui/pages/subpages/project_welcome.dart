import 'package:flutter/material.dart';

class ProjectWelcome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Project Welcome"),
    );
  }
}
